'use client';

import './Navbar.css';

const Navbar: React.FC = () => {
    return (
        <div className="navbar">
            <label className="brand">Creble</label>
        </div>
    );
};

export default Navbar;